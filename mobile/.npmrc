# React Native / Expo dependency trees frequently have peer-dependency ranges
# that strict npm v7+ resolution rejects (e.g. a navigation v6→v7 transition).
# legacy-peer-deps installs the versions package.json asks for instead of erroring.
legacy-peer-deps=true
